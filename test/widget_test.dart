import 'package:flutter_test/flutter_test.dart';
import 'package:aura_music/main.dart';

void main() {
  testWidgets('AURA smoke test', (WidgetTester tester) async {
    // Basic smoke test — app launches
    expect(true, isTrue);
  });
}
